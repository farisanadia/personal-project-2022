import { useRouter } from "next/router";

export default function Chat() {
    const checkUser = async() => {
        if (loading) return;
        if (!user) 
    }
    
    return (
        <div>
            hehe
        </div>
    )
}